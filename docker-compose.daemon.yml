services:
  wormhole-daemon:
    build:
      context: .
      dockerfile: Dockerfile.daemon
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
